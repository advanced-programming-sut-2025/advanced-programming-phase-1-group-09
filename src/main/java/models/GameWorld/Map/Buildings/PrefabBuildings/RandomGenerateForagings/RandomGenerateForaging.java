package models.GameWorld.Map.Buildings.PrefabBuildings.RandomGenerateForagings;

import models.GameWorld.Map.Buildings.Building;

public class RandomGenerateForaging extends Building {
    public RandomGenerateForaging(int width, int height) {
        super(width, height);
    }
}
